# Comandos para Despliegue en GitHub - SICADFOC 2026

# 1. Verificar archivos obligatorios
echo "Verificando archivos obligatorios..."
ls -la FOC26.py seguridad.py formacion_complementaria.py transacciones.py
ls -la gestion_estudiantil.py configuracion.py conexion_postgresql.py ui_estilos.py version.py
ls -la database/FOC26DB.sql requirements.txt Procfile runtime.txt
ls -la README.md DEPLOY_RAILWAY.md .gitignore

# 2. Inicializar Git (si no está hecho)
git init

# 3. Agregar archivos obligatorios
echo "Agregando archivos obligatorios..."
git add FOC26.py seguridad.py formacion_complementaria.py transacciones.py
git add gestion_estudiantil.py configuracion.py conexion_postgresql.py ui_estilos.py version.py
git add database/FOC26DB.sql
git add requirements.txt Procfile runtime.txt
git add README.md DEPLOY_RAILWAY.md
git add .gitignore .gitattributes

# 4. Agregar archivos opcionales útiles
echo "Agregando archivos opcionales útiles..."
git add setup_railway_simple.py seed_data.py
git add DEPLOYMENT_GUIDE.md DEPLOYMENT_FILES.md

# 5. Verificar estado
echo "Verificando estado de Git..."
git status

# 6. Commit inicial
echo "Realizando commit inicial..."
git commit -m "SICADFOC 2026 - Ready for Railway deployment

- Aplicación principal con todos los módulos
- Configuración dinámica para Railway
- Base de datos PostgreSQL con tablas creadas
- Datos de prueba funcionales
- Documentación completa de despliegue
- Archivos de configuración para producción"

# 7. Configurar repositorio remoto
echo "Configurando repositorio remoto..."
git branch -M main
git remote add origin https://github.com/tu-usuario/sicad-foc26.git

# 8. Push a GitHub
echo "Subiendo a GitHub..."
git push -u origin main

# 9. Verificar archivos subidos
echo "Verificando archivos subidos..."
git ls-tree --name-only HEAD

# 10. Configurar Railway
echo "Configurar en Railway:"
echo "1. Conectar repositorio a Railway"
echo "2. Configurar variable DATABASE_URL"
echo "3. Establecer PORT=8501"
echo "4. Desplegar automáticamente"

# Variables de entorno para Railway:
# DATABASE_URL=postgresql://postgres:UbZFWoDDCUyhbSrVhWVOHsmWqJhpjjop@hopper.proxy.rlwy.net:38358/railway
# PORT=8501
