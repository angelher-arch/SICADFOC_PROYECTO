# SICADFOC 26 - Requirements para Despliegue Docker
# Versiones fijas para evitar conflictos en el contenedor

# Core Framework
streamlit==1.37.0

# Database (con libpq-dev instalado en Dockerfile)
psycopg2-binary==2.9.6
python-dotenv==1.0.0

# Data Processing (versiones estables con binarios disponibles)
pandas==2.0.3
numpy==1.24.2

# Image Processing & OCR
Pillow==10.0.0
pytesseract==0.3.10

# Security & Authentication
bcrypt==4.0.1

# Utilities
python-dateutil==2.8.2
urllib3==2.0.4

# NOTA: Se excluyen herramientas de desarrollo para reducir tamaño del contenedor
# pytest, black, opencv-python-headless, etc. no se incluyen en producción
