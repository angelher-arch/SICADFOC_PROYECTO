# Variables de entorno para desarrollo local
# Copiar este archivo a .env y ajustar los valores

# Base de datos local
DB_HOST=localhost
DB_PORT=5432
DB_NAME=db_foc26
DB_USER=postgres
DB_PASSWORD=admin123

# Configuración de aplicación
APP_ENV=development
APP_DEBUG=true
APP_LOG_LEVEL=INFO

# Variables de producción (Render) - NO CAMBIAR
# Estas se configuran automáticamente en el servidor
# DATABASE_URL=postgresql://user:password@host:port/database
# RENDER=true
# RENDER_SERVICE_NAME=sicad-foc26
